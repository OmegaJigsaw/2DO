/**
 * Para usar un operador ternario en JS se debe declarar la condicion
 * de acceso en el caso de abajo la edad necesaria luego de eso
 * acompañar con el signo de (?) para luego colocar
 * primero la opcion de TRUE seguido de (:) y luego la opcion caso de FALSE
 */

let edad = 21;
let acceso = edad > 17 ? 'Permitir ingreso ' : 'No puede ingresar';
console.log(acceso);